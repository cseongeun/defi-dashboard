import TokenPriceScheduler from './tokenPrice.scheduler';

describe('TokenPriceScheduler', () => {
  beforeAll(async () => {
    await TokenPriceScheduler.init();
  });
  describe('getMultiTokenComposedofTarget', () => {
    it('');
  });
});
