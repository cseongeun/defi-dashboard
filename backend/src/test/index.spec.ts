enum STATUS {
  ACTIVATE,
  DEACTIVATE,
}

test('hello', async () => {
  const res = Object.values(STATUS);
  console.log(res);
});

it('asdfasdf', () => {
  class hello {
    name: this;
    getname() {
      return this;
    }
  }
});
